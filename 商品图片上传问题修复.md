# 商品图片上传问题修复

## 问题描述

管理员在编辑商品时，上传修改图片后，图片还是原来的图片。

## 根本原因

有两个问题：

### 1. 前端状态管理问题

在 `client/src/pages/Admin.tsx` 中：
- 使用 `defaultValue` 设置表单初始值，但这只在组件首次渲染时生效
- 当上传新图片时，`imagePreview` 状态被更新，但没有正确传递到保存逻辑
- 点击"编辑"按钮时，`imagePreview` 没有被重置

### 2. 后端缺少图片上传 API

当前系统没有实现图片上传功能：
- 前端代码尝试调用 `/api/upload/product-image`
- 但后端没有这个路由
- 所有商品图片都是静态文件，存储在 `client/public/images` 目录

## ✅ 已修复的问题

### 修复 1: 前端状态管理

**修改文件**: `client/src/pages/Admin.tsx`

1. **修复保存逻辑**（第 189 行）：
   ```typescript
   // 使用 imagePreview（如果有新上传的图片），否则使用表单值
   const imageValue = imagePreview || (formData.get('image') as string);
   
   const product = {
     // ...
     image: imageValue,
     // ...
   };
   ```

2. **修复编辑按钮**（第 485 行）：
   ```typescript
   <button onClick={() => { 
     setEditingProduct(product); 
     setImagePreview(null); // 重置图片预览
     setShowProductForm(true); 
   }} className="btn-edit">编辑</button>
   ```

3. **修复图片上传 URL**（第 228 行）：
   ```typescript
   // 使用环境变量而不是硬编码 localhost
   const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:8787';
   const response = await fetch(`${API_BASE_URL}/api/upload/product-image`, {
     // ...
     headers: {
       'Authorization': `Bearer ${localStorage.getItem('auth_token')}`
     }
   });
   ```

## ⚠️ 仍需解决的问题

### 后端图片上传 API 缺失

当前系统没有图片上传功能。有两个解决方案：

### 方案 1: 使用静态图片路径（临时方案）

**适用场景**: 快速修复，图片数量不多

**操作步骤**:
1. 将图片手动上传到 `client/public/images/products/` 目录
2. 在管理后台编辑商品时，直接输入图片路径
3. 例如: `/images/products/my-product.jpg`

**优点**:
- 无需开发后端 API
- 简单快速

**缺点**:
- 需要手动管理图片文件
- 无法通过管理后台直接上传
- 需要重新部署前端才能添加新图片

### 方案 2: 实现完整的图片上传功能（推荐）

**适用场景**: 长期使用，需要动态上传图片

需要实现：

#### 2.1 后端 API（使用 Cloudflare R2）

创建 `workers/src/routes/upload.ts`:

```typescript
import { Hono } from 'hono';
import { requireAdmin } from '../middleware/auth';
import type { Env } from '../types/env';

const upload = new Hono<{ Bindings: Env }>();

// Upload product image
upload.post('/product-image', requireAdmin, async (c) => {
  try {
    const formData = await c.req.formData();
    const file = formData.get('image') as File;
    
    if (!file) {
      return c.json({ success: false, error: 'No file provided' }, 400);
    }

    // Validate file type
    if (!file.type.startsWith('image/')) {
      return c.json({ success: false, error: 'File must be an image' }, 400);
    }

    // Validate file size (max 5MB)
    if (file.size > 5 * 1024 * 1024) {
      return c.json({ success: false, error: 'File size must be less than 5MB' }, 400);
    }

    // Generate unique filename
    const timestamp = Date.now();
    const extension = file.name.split('.').pop();
    const filename = `products/${timestamp}.${extension}`;

    // Upload to R2
    const arrayBuffer = await file.arrayBuffer();
    await c.env.R2_BUCKET.put(filename, arrayBuffer, {
      httpMetadata: {
        contentType: file.type,
      },
    });

    // Return public URL
    const publicUrl = `https://your-r2-domain.com/${filename}`;
    
    return c.json({ 
      success: true, 
      path: publicUrl,
      filename 
    });
  } catch (error) {
    console.error('Upload error:', error);
    return c.json({ 
      success: false, 
      error: 'Upload failed' 
    }, 500);
  }
});

export { upload };
```

#### 2.2 配置 Cloudflare R2

在 `workers/wrangler.toml` 中添加：

```toml
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "ecommerce-uploads"
```

#### 2.3 在主应用中注册路由

在 `workers/src/index.ts` 中添加：

```typescript
import { upload } from './routes/upload';

// ...
app.route('/api/upload', upload);
```

#### 2.4 设置 R2 公开访问

1. 在 Cloudflare Dashboard 中创建 R2 bucket
2. 配置自定义域名或使用 R2.dev 域名
3. 设置 CORS 规则允许前端访问

### 方案 3: 使用第三方图片托管（简单方案）

**适用场景**: 不想管理存储，快速实现

**选项**:
- Cloudinary
- ImgBB
- Imgur API
- AWS S3

**实现**:
1. 注册第三方服务
2. 获取 API 密钥
3. 在后端实现上传代理
4. 返回图片 URL

## 🚀 快速测试

### 测试修复后的编辑功能

1. 访问管理后台：https://seedlight.tech/admin
2. 点击"商品管理"标签
3. 点击任意商品的"编辑"按钮
4. 修改商品信息（不上传新图片）
5. 点击"保存"
6. 确认修改成功

### 测试图片路径修改

1. 编辑商品
2. 在图片上传区域下方，有一个隐藏的 input
3. 可以直接修改图片路径
4. 例如改为: `/images/products/productivity/productivity-1.jpg`
5. 保存后查看商品列表，图片应该更新

## 📋 当前图片管理方式

### 现有图片位置

```
client/public/images/
├── products/
│   ├── mobility/
│   │   ├── mobility-1.jpg
│   │   ├── mobility-2.jpg
│   │   └── ...
│   ├── productivity/
│   │   ├── productivity-1.jpg
│   │   ├── productivity-2.jpg
│   │   └── ...
│   ├── sanctuary/
│   │   └── ...
│   └── savoriness/
│       └── ...
└── burst-products/
    └── ...
```

### 添加新图片的步骤

1. 将图片文件放到对应的分类目录
2. 在管理后台编辑商品
3. 输入图片路径: `/images/products/[category]/[filename].jpg`
4. 保存

### 图片命名规范

建议使用以下格式：
- `[category]-[number].jpg` (例如: `mobility-16.jpg`)
- `[product-name]-[variant].jpg` (例如: `wallet-black.jpg`)

## 🔧 部署更新

修改已提交到代码库，需要重新部署前端：

```bash
# 1. 提交更改
git add client/src/pages/Admin.tsx
git commit -m "fix: resolve product image update issue in admin panel"
git push origin main

# 2. Cloudflare Pages 会自动部署
# 或手动触发部署
```

## 📝 后续建议

1. **短期**: 使用静态图片路径，手动管理图片文件
2. **中期**: 实现 R2 图片上传功能
3. **长期**: 考虑图片优化、CDN、缩略图生成等功能

## ✅ 验证清单

- [x] 修复前端状态管理问题
- [x] 修复编辑时图片预览重置
- [x] 修复图片上传 API URL
- [ ] 实现后端图片上传 API（待开发）
- [ ] 配置 Cloudflare R2 存储（待配置）

---

**修复时间**: 2026-02-23
**影响范围**: 管理后台商品编辑功能
**状态**: 前端已修复，后端图片上传待实现
