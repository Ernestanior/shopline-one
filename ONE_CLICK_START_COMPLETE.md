# ✅ 一键启动配置完成

## 🎉 完成内容

### 1. 端口配置 ✅
- **后端端口**: 5001 → 5002
- **前端端口**: 3000 → 3001

### 2. 环境配置文件 ✅

#### `.env` (后端)
```env
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=shop_dev
DB_PASSWORD=
DB_NAME=shop
PORT=5002
JWT_SECRET=xyvn_shop_secret_key_2026
JWT_EXPIRES_IN=7d
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3001
```

#### `client/.env` (前端)
```env
PORT=3001
REACT_APP_API_URL=http://localhost:5002
```

### 3. 启动脚本 ✅

#### `start.sh` (macOS/Linux)
- ✅ 自动检查环境（Node.js, MySQL）
- ✅ 自动初始化数据库
- ✅ 自动安装依赖
- ✅ 自动启动后端和前端
- ✅ 彩色输出，友好提示
- ✅ Ctrl+C 优雅退出

#### `start.bat` (Windows)
- ✅ 自动检查环境
- ✅ 自动初始化数据库
- ✅ 自动安装依赖
- ✅ 自动启动后端和前端
- ✅ 分别在新窗口中运行

### 4. package.json 更新 ✅
```json
{
  "scripts": {
    "start": "node server/init-database.js && npm run dev",
    "start:unix": "bash start.sh",
    "start:win": "start.bat"
  }
}
```

### 5. 代码更新 ✅
- ✅ 更新 `server/index.js` 默认端口为 5002
- ✅ 更新 `client/package.json` proxy 为 5002
- ✅ 更新 `client/src/pages/Checkout.tsx` 使用相对路径
- ✅ 更新测试脚本端口

### 6. 文档创建 ✅
- ✅ `README.md` - 项目主文档
- ✅ `START_HERE.md` - 快速开始
- ✅ `QUICK_START.md` - 详细启动指南

---

## 🚀 使用方法

### 方法1: 使用启动脚本（推荐）

**macOS/Linux:**
```bash
./start.sh
```

**Windows:**
```bash
start.bat
```

### 方法2: 使用 npm

```bash
npm start
```

### 方法3: 使用 npm 脚本

**macOS/Linux:**
```bash
npm run start:unix
```

**Windows:**
```bash
npm run start:win
```

---

## 📍 访问地址

启动后访问：

- **官网**: http://localhost:3001
- **管理后台**: http://localhost:3001/admin
- **后端API**: http://localhost:5002

---

## 👤 默认账户

```
邮箱: admin@xyvn.com
密码: admin123
```

---

## ✨ 启动脚本功能

### 自动化流程
1. ✅ 检查 Node.js 是否安装
2. ✅ 检查 MySQL 是否安装
3. ✅ 初始化数据库（创建表、插入数据）
4. ✅ 检查并安装依赖
5. ✅ 启动后端服务（端口 5002）
6. ✅ 启动前端服务（端口 3001）

### 友好提示
- ✅ 彩色输出（绿色=成功，黄色=警告，红色=错误）
- ✅ 进度显示（[1/5], [2/5], ...）
- ✅ 清晰的访问地址提示
- ✅ 默认账户信息提示
- ✅ 错误处理和建议

### 优雅退出
- ✅ Ctrl+C 停止所有服务
- ✅ 自动清理后台进程

---

## 🧪 测试

启动后可以运行测试脚本：

```bash
# 测试反馈和订阅系统
node test-feedback-system.js

# 测试新API
node test-new-apis.js
```

---

## 🔧 自定义端口

如需修改端口，编辑以下文件：

### 后端端口
编辑 `.env`:
```env
PORT=你的端口号
```

### 前端端口
编辑 `client/.env`:
```env
PORT=你的端口号
```

同时更新 `client/package.json` 中的 proxy 配置。

---

## 📊 目录结构

```
zenlet/
├── start.sh              # macOS/Linux 启动脚本
├── start.bat             # Windows 启动脚本
├── .env                  # 后端环境配置
├── README.md             # 项目主文档
├── START_HERE.md         # 快速开始
├── QUICK_START.md        # 详细启动指南
├── package.json          # 后端依赖和脚本
├── server/
│   ├── index.js         # 后端主文件（端口 5002）
│   ├── admin-api.js     # 管理员API
│   └── init-database.js # 数据库初始化
├── client/
│   ├── .env             # 前端环境配置
│   ├── package.json     # 前端依赖（proxy: 5002）
│   └── src/
│       └── pages/
│           └── Checkout.tsx  # 使用相对路径
└── test-*.js            # 测试脚本（端口 5002）
```

---

## ✅ 验证清单

- [x] 后端默认端口改为 5002
- [x] 前端默认端口改为 3001
- [x] 创建 .env 配置文件
- [x] 创建 client/.env 配置文件
- [x] 更新 client/package.json proxy
- [x] 更新 Checkout.tsx API 调用
- [x] 更新测试脚本端口
- [x] 创建 start.sh 脚本
- [x] 创建 start.bat 脚本
- [x] 添加 npm start 命令
- [x] 创建 README.md
- [x] 创建 START_HERE.md
- [x] 更新 QUICK_START.md
- [x] 数据库初始化成功
- [x] 所有代码无错误

---

## 🎯 下一步

1. 运行启动脚本
2. 访问 http://localhost:3001
3. 使用管理员账户登录
4. 开始使用系统

---

**配置完成日期**: 2026-02-11  
**状态**: ✅ 完成并测试通过  
**一键启动**: ✅ 可用
