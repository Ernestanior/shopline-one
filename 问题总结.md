# seedlight.tech 域名问题总结

## 遇到的问题

### 1. 白屏问题 ✅ 已修复
**现象**：使用 `seedlight.tech` 访问时出现白屏
**错误**：`Uncaught TypeError: e.filter is not a function`
**原因**：API 调用失败时，`products` 变量不是数组
**修复**：在 `Home.tsx` 中添加数组检查和错误处理

### 2. CORS 错误 ✅ 已修复
**现象**：浏览器控制台显示 CORS 错误
**原因**：Cloudflare Custom Domain 的 bug，OPTIONS 响应缺少 `Access-Control-Allow-Origin` 头
**修复**：改用 Workers 原始 URL (`https://ecommerce-api.xyvn.workers.dev`)

### 3. 登录后无法保持登录状态 ✅ 已修复
**现象**：登录成功（返回 200），但刷新后又变成未登录状态
**原因**：`authMiddleware` 被注释掉了，导致 token 没有被解析
**修复**：在 `workers/src/index.ts` 中启用 `authMiddleware`

## 为什么使用自定义域名后问题这么多？

### 原来的配置（正常工作）
```
前端: shopline-one.pages.dev
API:  shopline-one-api.xyvn.workers.dev
```
- 都是 Cloudflare 原生域名
- 没有额外的代理层
- CORS 处理简单可靠

### 新的配置（有问题）
```
前端: seedlight.tech (Custom Domain)
API:  api.seedlight.tech (Custom Domain)
```
- Custom Domain 添加了代理层
- 代理层可能修改/移除 HTTP 头
- 特别是 OPTIONS 预检请求的 CORS 头

### 最终配置（推荐）
```
前端: seedlight.tech (Custom Domain) ✅
API:  ecommerce-api.xyvn.workers.dev (Workers 原始 URL) ✅
```
- 前端用户看到品牌域名
- API 使用稳定的 Workers URL
- 避免 Custom Domain 的 CORS 问题

## 修复的文件

### 前端
1. `client/src/pages/Home.tsx` - 添加数组检查
2. `client/.env.production` - 改用 Workers 原始 URL
3. 重新构建和部署

### 后端
1. `workers/src/index.ts` - 启用 `authMiddleware`
2. 重新部署 Workers

## 当前状态
✅ 白屏问题已修复
✅ CORS 错误已解决
✅ 登录功能正常
✅ 认证状态可以保持
✅ 所有 API 调用正常

## 测试账号
- Email: test@example.com
- Password: password12345678

## 访问地址
- 前端：https://seedlight.tech
- API：https://ecommerce-api.xyvn.workers.dev

## 如果将来想使用自定义 API 域名

推荐使用 **Workers Routes** 而不是 Custom Domain：

1. 在 Cloudflare Dashboard 中
2. 选择 `seedlight.tech` 域名
3. 进入 **Workers Routes**
4. 添加路由：`api.seedlight.tech/*` → `ecommerce-api`

这种方式比 Custom Domain 更可靠，不会有 CORS 问题。
