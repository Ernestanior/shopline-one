# 部署指南 - 图片上传修复

## 修改内容总结

### 1. 前端修改（立即生效）
**文件：`client/src/pages/Admin.tsx`**
- 修改了 `handleImageUpload` 函数
- 现在直接在浏览器中将图片转换为 base64
- 添加了文件类型和大小验证（最大 2MB）
- 不再依赖后端上传端点

### 2. 后端修改（需要部署）
**文件：`workers/src/routes/admin.ts`**
- 添加了 `/api/admin/upload/product-image` 端点
- 支持文件上传和 base64 转换
- 包含服务器端验证

**文件：`workers/src/index.ts`**
- 注册了 `/api/upload` 路由

## 快速部署

### 选项 1：只部署前端（推荐，最快）

前端修改已经足够解决问题，因为现在图片在浏览器中直接转换：

```bash
cd client
npm run build
```

然后部署到 Cloudflare Pages：
- 方式 A：通过 Git 推送（自动部署）
- 方式 B：使用 Wrangler CLI
  ```bash
  npx wrangler pages deploy build --project-name=seedlight
  ```

### 选项 2：同时部署前端和后端（完整方案）

#### 步骤 1：部署后端
```bash
cd workers
npm run deploy
# 或
npx wrangler deploy
```

#### 步骤 2：部署前端
```bash
cd client
npm run build
npx wrangler pages deploy build --project-name=seedlight
```

## 验证部署

### 1. 检查前端部署
访问：`https://seedlight.tech/admin`

### 2. 测试图片上传

#### 测试新增商品
1. 登录管理后台
2. 点击"添加商品"
3. 上传图片
4. 应该立即看到图片预览
5. 保存商品
6. 刷新页面确认图片显示

#### 测试编辑商品
1. 点击任意商品的"编辑"按钮
2. 看到原有图片
3. 点击文件选择器，选择新图片
4. 应该立即看到新图片预览
5. 点击"保存"
6. 刷新页面确认图片已更新

### 3. 检查后端端点（如果部署了后端）
```bash
# 测试上传端点
curl -X POST https://ecommerce-api.xyvn.workers.dev/api/upload/product-image \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "image=@test-image.jpg"
```

应该返回：
```json
{
  "success": true,
  "path": "data:image/jpeg;base64,...",
  "filename": "test-image.jpg",
  "size": 12345,
  "type": "image/jpeg"
}
```

## 常见问题

### Q1: 部署后图片还是不能更新？
**A:** 清除浏览器缓存，或使用隐私模式重新测试。

### Q2: 上传大图片失败？
**A:** 当前限制是 2MB。如果需要更大的图片：
1. 修改 `Admin.tsx` 中的大小限制
2. 考虑在前端压缩图片
3. 或者实现 R2 存储方案

### Q3: 图片显示为损坏的图标？
**A:** 检查：
1. 图片是否成功保存到数据库
2. 数据库字段是否足够大（TEXT 类型）
3. Base64 字符串是否完整

### Q4: 前端方案和后端方案有什么区别？
**A:** 
- **前端方案**：图片在浏览器中转换，不经过服务器，立即可用
- **后端方案**：图片上传到服务器处理，提供统一的验证和错误处理

两种方案都将图片转换为 base64 存储在数据库中。

## 回滚方案

如果部署后出现问题，可以回滚：

### 回滚前端
```bash
# 在 Cloudflare Pages Dashboard 中
# 1. 进入 seedlight 项目
# 2. 点击 "Deployments"
# 3. 找到之前的部署
# 4. 点击 "Rollback to this deployment"
```

### 回滚后端
```bash
cd workers
git checkout HEAD~1 src/routes/admin.ts
git checkout HEAD~1 src/index.ts
npm run deploy
```

## 监控和日志

### 查看 Workers 日志
```bash
npx wrangler tail
```

### 查看 Pages 部署日志
在 Cloudflare Dashboard > Pages > seedlight > Deployments

## 下一步优化

部署成功后，可以考虑：

1. **图片压缩**：在前端上传前压缩图片
2. **R2 存储**：迁移到 Cloudflare R2 对象存储
3. **图片优化**：使用 Cloudflare Images 服务
4. **CDN 缓存**：优化图片加载速度

## 需要帮助？

如果遇到问题：
1. 检查浏览器控制台的错误信息
2. 查看 Workers 日志：`npx wrangler tail`
3. 确认环境变量配置正确
4. 验证认证 token 有效

---

**当前状态：** ✅ 代码已修改，等待部署
**推荐方案：** 先部署前端（选项 1），测试成功后再考虑部署后端
