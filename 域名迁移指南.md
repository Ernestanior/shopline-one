# 将 Bluehost 域名迁移到 Cloudflare Pages 指南

## 概述

本指南将帮助你将 Bluehost 上的域名迁移到 Cloudflare Pages 项目（shopline-one.pages.dev）。

## 前提条件

- 你在 Bluehost 上拥有域名的管理权限
- 你的 Cloudflare Pages 项目已部署（shopline-one.pages.dev）
- 你有 Cloudflare 账号的访问权限

## 方案选择

有两种方式可以将域名连接到 Cloudflare Pages：

### 方案 A：仅添加自定义域名（推荐，快速）
- 保持域名在 Bluehost 注册
- 只修改 DNS 记录指向 Cloudflare Pages
- 适合：想快速上线，不想转移域名注册商

### 方案 B：完全迁移到 Cloudflare
- 将域名注册商也转移到 Cloudflare
- 获得 Cloudflare 的完整 DNS 管理和保护
- 适合：想要统一管理，获得更好的性能和安全性

---

## 方案 A：仅添加自定义域名（推荐）

### 第 1 步：在 Cloudflare Pages 添加自定义域名

1. 登录 Cloudflare Dashboard：https://dash.cloudflare.com
2. 进入 **Workers & Pages** 
3. 选择你的项目 **shopline-one**
4. 点击 **Custom domains** 标签
5. 点击 **Set up a custom domain** 按钮
6. 输入你的域名（例如：yourdomain.com）
7. 点击 **Continue**

Cloudflare 会显示需要配置的 DNS 记录。

### 第 2 步：在 Bluehost 配置 DNS 记录

#### 2.1 登录 Bluehost

1. 访问 https://my.bluehost.com
2. 登录你的账号

#### 2.2 进入 DNS 管理

1. 在控制面板中找到 **Domains**
2. 选择你要迁移的域名
3. 点击 **DNS** 或 **Manage DNS**

#### 2.3 添加/修改 DNS 记录

根据 Cloudflare 显示的记录类型，配置相应的 DNS：

**如果是根域名（例如：yourdomain.com）：**

删除现有的 A 记录，添加新的 CNAME 记录：
- **Type**: CNAME
- **Host**: @ 或留空
- **Points to**: shopline-one.pages.dev
- **TTL**: 自动或 3600

**如果是子域名（例如：www.yourdomain.com）：**

添加 CNAME 记录：
- **Type**: CNAME
- **Host**: www
- **Points to**: shopline-one.pages.dev
- **TTL**: 自动或 3600

**注意**：某些 DNS 提供商不允许根域名使用 CNAME，这种情况下需要使用 A 记录：
- **Type**: A
- **Host**: @ 或留空
- **Points to**: （Cloudflare 会提供 IP 地址）
- **TTL**: 自动或 3600

#### 2.4 保存更改

点击 **Save** 或 **Add Record** 保存 DNS 配置。

### 第 3 步：等待 DNS 传播

- DNS 更改通常需要 5-30 分钟生效
- 最长可能需要 24-48 小时完全传播
- 你可以使用工具检查 DNS 传播状态：https://dnschecker.org

### 第 4 步：在 Cloudflare 验证域名

1. 返回 Cloudflare Pages 的 Custom domains 页面
2. 等待域名状态变为 **Active**
3. Cloudflare 会自动为你的域名配置 SSL 证书（Let's Encrypt）

### 第 5 步：测试域名

在浏览器中访问你的域名，确认网站正常显示。

---

## 方案 B：完全迁移到 Cloudflare（可选）

如果你想获得更好的性能和管理体验，可以将域名完全迁移到 Cloudflare。

### 第 1 步：将域名添加到 Cloudflare

1. 登录 Cloudflare Dashboard
2. 点击 **Add a Site**
3. 输入你的域名
4. 选择免费计划（Free Plan）
5. 点击 **Continue**

### 第 2 步：Cloudflare 扫描现有 DNS 记录

Cloudflare 会自动扫描你在 Bluehost 的 DNS 记录并导入。

1. 检查导入的记录是否正确
2. 点击 **Continue**

### 第 3 步：更改域名服务器（Nameservers）

Cloudflare 会显示两个 nameserver 地址，例如：
```
ns1.cloudflare.com
ns2.cloudflare.com
```

#### 3.1 在 Bluehost 更改 Nameservers

1. 登录 Bluehost
2. 进入 **Domains** 管理
3. 选择你的域名
4. 找到 **Nameservers** 设置
5. 选择 **Custom Nameservers**
6. 输入 Cloudflare 提供的两个 nameserver
7. 保存更改

### 第 4 步：等待 Nameserver 更改生效

- 通常需要 2-24 小时
- Cloudflare 会通过邮件通知你更改已生效

### 第 5 步：在 Cloudflare Pages 添加自定义域名

1. 进入 **Workers & Pages**
2. 选择 **shopline-one** 项目
3. 点击 **Custom domains**
4. 添加你的域名
5. Cloudflare 会自动配置 DNS 记录

### 第 6 步：配置 SSL/TLS

1. 在 Cloudflare Dashboard 中选择你的域名
2. 进入 **SSL/TLS** 设置
3. 选择 **Full** 或 **Full (strict)** 模式
4. 等待 SSL 证书自动配置

---

## 常见问题

### Q1: DNS 更改后多久生效？
A: 通常 5-30 分钟，最长可能需要 48 小时。

### Q2: 更改期间网站会下线吗？
A: 在 DNS 传播期间，部分用户可能看到旧网站，部分看到新网站。建议在低流量时段操作。

### Q3: 如何确认 DNS 已经生效？
A: 使用命令行工具：
```bash
# macOS/Linux
nslookup yourdomain.com

# 或使用 dig
dig yourdomain.com
```

### Q4: SSL 证书需要手动配置吗？
A: 不需要，Cloudflare Pages 会自动为你的自定义域名配置免费的 SSL 证书。

### Q5: 可以同时使用 www 和非 www 域名吗？
A: 可以，在 Cloudflare Pages 中同时添加两个域名：
- yourdomain.com
- www.yourdomain.com

然后在 Cloudflare 的 **Rules** 中设置重定向规则。

---

## 更新 API 和前端配置

域名迁移后，需要更新项目中的域名配置：

### 1. 更新 Workers API 的 CORS 配置

编辑 `workers/wrangler.toml`：

```toml
[env.production.vars]
ALLOWED_ORIGINS = "https://yourdomain.com,https://www.yourdomain.com"
```

### 2. 更新前端 API 配置

编辑 `client/.env.production`：

```env
REACT_APP_API_URL=https://ecommerce-api.youraccount.workers.dev
```

### 3. 重新部署

```bash
# 部署前端
cd client
npm run build
npx wrangler pages deploy build

# 部署 Workers API
cd ../workers
npx wrangler deploy --env production
```

---

## 验证清单

完成迁移后，请验证以下项目：

- [ ] 域名可以正常访问
- [ ] HTTPS 证书正常工作（浏览器地址栏显示锁图标）
- [ ] 网站所有页面正常加载
- [ ] API 请求正常工作
- [ ] 图片和静态资源正常加载
- [ ] 登录/注册功能正常
- [ ] 购物车和结账流程正常

---

## 需要帮助？

如果在迁移过程中遇到问题，可以：

1. 检查 Cloudflare 的状态页面：https://www.cloudflarestatus.com
2. 查看 Cloudflare 文档：https://developers.cloudflare.com/pages
3. 联系 Cloudflare 支持（免费计划也有社区支持）

---

## 回滚方案

如果迁移后出现问题，可以快速回滚：

1. 在 Bluehost 中将 DNS 记录改回原来的设置
2. 等待 DNS 传播（5-30 分钟）
3. 网站会恢复到迁移前的状态
