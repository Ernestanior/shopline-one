# 支付认证问题 - 快速解决方案

## 问题

访问 `https://seedlight.tech/checkout/17` 点击"前往支付"时返回：
```json
{"error":"Unauthorized"}
```

## 最可能的原因

**你没有登录！**

支付功能需要用户认证，必须先登录才能创建支付订单。

## ✅ 立即解决（3 步骤）

### 1. 登录账户

访问：https://seedlight.tech/login

输入你的账号密码登录。

### 2. 确认登录成功

登录后，页面右上角应该显示你的用户名或邮箱。

### 3. 重新尝试支付

回到结账页面，点击"前往支付"。

## 🔍 如何确认是否已登录

打开浏览器开发者工具（F12），在 Console 中执行：

```javascript
// 检查是否有 token
console.log('已登录:', !!localStorage.getItem('auth_token'));

// 查看 token（如果存在）
console.log('Token:', localStorage.getItem('auth_token')?.substring(0, 50) + '...');
```

**结果**：
- ✅ `已登录: true` - 说明已登录
- ❌ `已登录: false` - 说明未登录，需要先登录

## 🧪 测试工具

我创建了一个测试页面，可以帮你诊断问题：

1. 在浏览器中打开 `test-payment-auth.html` 文件
2. 按照页面上的步骤测试：
   - 步骤 1: 登录获取 Token
   - 步骤 2: 验证 Token 是否有效
   - 步骤 3: 测试支付创建
   - 步骤 4: 检查 localStorage

## 🐛 其他可能的问题

### 问题 1: 已登录但仍然 Unauthorized

**原因**: Token 可能已过期

**解决**: 
1. 清除 localStorage：
   ```javascript
   localStorage.clear()
   ```
2. 刷新页面
3. 重新登录

### 问题 2: 登录后立即失效

**原因**: 浏览器隐私设置阻止 localStorage

**解决**:
- 不要使用无痕模式
- 检查浏览器隐私设置
- 允许网站保存数据

### 问题 3: CORS 错误

**症状**: Console 中看到 CORS 相关错误

**解决**: 
- 确保访问的是 `https://seedlight.tech`（不是 localhost）
- 清除浏览器缓存
- 尝试使用其他浏览器

## 📋 完整的支付流程

1. **注册/登录** → 获取认证 token
2. **浏览商品** → 添加到购物车
3. **进入结账** → 填写收货信息
4. **选择支付方式** → 蓝新金流或绿界科技
5. **创建支付** → 需要认证（这一步出错了）
6. **跳转支付页面** → 完成支付

## 🔐 认证技术细节

### 前端如何发送认证

`client/src/lib/api.ts` 中的 `apiFetch` 函数：

```typescript
// 从 localStorage 获取 token
const token = localStorage.getItem('auth_token');

// 添加到请求 header
headers: {
  'Authorization': `Bearer ${token}`
}
```

### 后端如何验证

`workers/src/middleware/auth.ts` 中的 `requireAuth`：

```typescript
// 1. 从 header 提取 token
const authHeader = c.req.header('Authorization');
const token = authHeader.substring(7); // 移除 'Bearer '

// 2. 验证 token
const user = await verifyToken(token, JWT_SECRET);

// 3. 如果无效，返回 401
if (!user) {
  return c.json({ error: 'Unauthorized' }, 401);
}
```

## 🚀 快速测试命令

在 https://seedlight.tech 的 Console 中执行：

```javascript
// 完整测试脚本
(async function() {
  console.log('=== 支付认证诊断 ===\n');
  
  // 1. 检查 token
  const token = localStorage.getItem('auth_token');
  console.log('1. Token 状态:', token ? '✅ 存在' : '❌ 不存在');
  
  if (!token) {
    console.log('\n👉 请先登录: https://seedlight.tech/login');
    return;
  }
  
  // 2. 验证认证
  console.log('\n2. 验证认证...');
  try {
    const authRes = await fetch('https://ecommerce-api.xyvn.workers.dev/api/auth/me', {
      headers: { 'Authorization': 'Bearer ' + token }
    });
    const authData = await authRes.json();
    
    if (authRes.ok) {
      console.log('✅ 认证成功');
      console.log('   用户:', authData.email);
      console.log('   ID:', authData.id);
    } else {
      console.log('❌ 认证失败:', authData.error);
      console.log('👉 Token 可能已过期，请重新登录');
      return;
    }
  } catch (err) {
    console.log('❌ 请求失败:', err.message);
    return;
  }
  
  // 3. 测试支付创建
  console.log('\n3. 测试支付创建...');
  try {
    const paymentRes = await fetch('https://ecommerce-api.xyvn.workers.dev/api/payment/create', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + token
      },
      body: JSON.stringify({
        orderId: '17',
        gateway: 'newebpay',
        paymentMethod: 'credit_card'
      })
    });
    const paymentData = await paymentRes.json();
    
    if (paymentRes.ok) {
      console.log('✅ 支付创建成功');
      console.log('   Transaction ID:', paymentData.transactionId);
    } else {
      console.log('❌ 支付创建失败:', paymentData.error);
    }
  } catch (err) {
    console.log('❌ 请求失败:', err.message);
  }
  
  console.log('\n=== 诊断完成 ===');
})();
```

## 📞 仍然有问题？

如果按照以上步骤仍然无法解决：

1. 提供以下信息：
   - 是否已登录？
   - Console 中的完整错误信息
   - Network 标签中的请求详情

2. 检查：
   - 浏览器版本
   - 是否使用 VPN
   - 是否有浏览器扩展阻止请求

---

**最可能的解决方案**: 先登录，然后再尝试支付
**测试工具**: test-payment-auth.html
**创建时间**: 2026-02-23
